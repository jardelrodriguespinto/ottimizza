services:
  app1:
    build: .
    ports:
      - "8007:8080"
    environment:
      - SPRING_DATASOURCE_URL=jdbc:postgresql://db:5433/teste
      - REDIS_HOST=redis
      - SPRING_RABBITMQ_HOST=rabbitmq
    depends_on:
      - db
      - redis
      - rabbitmq
      
  app2:
    build: .
    ports:
      - "8008:8080"
    environment:
      - SPRING_DATASOURCE_URL=jdbc:postgresql://db:5433/teste
      - REDIS_HOST=redis
      - SPRING_RABBITMQ_HOST=rabbitmq
    depends_on:
      - db
      - redis
      - rabbitmq

  db:
    image: postgres:16
    environment:
      - POSTGRES_DB=teste
      - POSTGRES_USER=user
      - POSTGRES_PASSWORD=password
    ports:
      - "5433:5432"

  redis:
    image: redis:7
    ports:
      - "6379:6379"

  rabbitmq:
    image: rabbitmq:3-management
    ports:
      - "127.0.0.1:5672:5672"
      - "127.0.0.1:15672:15672"

  nginx:
    image: nginx:alpine
    ports:
      - "80:80"
    volumes:
      - ./nginx.conf:/etc/nginx/conf.d/default.conf:ro
    depends_on:
      - app1
      - app2
